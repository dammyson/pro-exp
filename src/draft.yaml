models:
  CampaignRule:
    id: id
    name: string
    relationships:
      belongsToMany: Campaign

controllers:
  CampaignRule:
    index:
      query: all

    store:
      validate: name, 
      save: Company

